<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>DRPC.Attribute</PackageId>
    <Description>DRPC 계약 메서드에 붙이는 특성(RemoteProcedure)과 전송 방식(RpcDeliveryMode). 런타임 의존성 없음. Unity(netstandard2.1) 호환.</Description>
    <RootNamespace>DRPC</RootNamespace>
  </PropertyGroup>

</Project>
